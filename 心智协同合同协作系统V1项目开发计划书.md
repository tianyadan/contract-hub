# 心智协同合同协作系统 V1 项目开发计划书

编写日期：2026-08-16

## 1. 项目背景

心智协同合同协作系统 V1 是给建筑资质代办业务使用的轻量合同协作 Web 系统。

系统目标不是做大型 OA、CRM 或在线 Office，而是先把合同从导入、编辑、版本记录、分享协作、确认到最终 DOCX 下载的业务闭环跑通。

V1 的核心原则：

- 轻量：服务数量少，部署简单，功能聚焦。
- 安全：用户权限、分享链接、文件校验、审计日志必须落地。
- 效率：先完成真实业务链路，避免过度架构。

## 2. V1 功能范围

### 2.1 必须实现

- 用户登录。
- 导入 DOCX 文档。
- 在线编辑文档内容。
- 保存后生成版本号。
- 保存版本之间的变更记录。
- 生成分享链接，邀请外部客户协作。
- 阿里云 OSS 文件存储。
- 首页 Dashboard 展示合同数量和状态。
- 合同确认。
- 最终 DOCX 下载。

### 2.2 合同状态

V1 状态建议：

- 已导入：`IMPORTED`
- 已分享：`SHARED`
- 协作中：`COLLABORATING`
- 已确认：`CONFIRMED`
- 已完成：`COMPLETED`

状态后续可以扩展，但 V1 先不要设计过多状态。

### 2.3 明确不做

- 复杂 RBAC。
- 微服务拆分。
- 电子签章。
- OCR。
- AI 合同审核。
- 实时多人光标 / CRDT。
- 复杂 CRM。

## 3. 已有数据库表

数据库表已经建好，V1 不重新设计 SQL。

已有表：

- `sys_user`
- `contract`
- `contract_version`
- `contract_collaborator`
- `contract_share`
- `contract_change`
- `contract_confirmation`
- `contract_audit_log`

开发时直接围绕这些表实现业务。

## 4. 用户角色

### 4.1 系统用户

系统用户通过账号密码登录。

系统用户可以：

- 创建合同。
- 导入 DOCX。
- 编辑合同。
- 生成版本。
- 创建分享链接。
- 确认合同。
- 下载最终 DOCX。
- 查看 Dashboard。

### 4.2 合同创建人 OWNER

合同创建人拥有该合同的完整管理权限。

OWNER 可以：

- 编辑合同。
- 分享合同。
- 查看版本和变更记录。
- 邀请外部客户协作。
- 确认合同。
- 完成合同。

### 4.3 外部协作者 COLLABORATOR

外部客户通过分享链接进入。

V1 不强制客户注册账号，降低使用门槛。

外部协作者可以：

- 输入姓名进入分享页。
- 查看合同。
- 提交修改。
- 确认当前版本。

## 5. 技术选型

### 5.1 前端

技术：

- React
- TypeScript
- Vite
- Ant Design

选择原因：

- React + TypeScript 生态成熟。
- 类型提示清晰，适合初学阶段降低错误。
- Ant Design 适合后台管理类系统。
- Vite 启动和构建速度快。

### 5.2 业务后端

技术：

- Go

Go 负责：

- 用户登录。
- JWT 鉴权。
- 合同业务。
- 权限判断。
- 版本管理。
- 分享链接。
- 协作者。
- 合同确认。
- 审计日志。
- Dashboard。
- 阿里云 OSS。
- 调用 Python 文档服务。
- Swagger 接口文档自动生成。

选择原因：

- 部署简单，一个二进制文件即可运行。
- 性能好。
- 适合作为统一业务入口。
- 后期用 systemd 管理简单可靠。
- 使用 `swaggo/swag` + `gin-swagger`，通过接口注释自动生成和维护 API 文档。

### 5.3 文档处理后端

技术：

- Python 3.12+
- FastAPI
- python-docx
- uvicorn

Python 负责：

- DOCX 解析。
- 文档结构化 JSON 生成。
- 两个版本之间的 Diff。
- 根据结构化 JSON 渲染最终 DOCX。
- V1 支持页眉、页脚。
- V1 支持 Word 公式提取。
- V1 暂不做图片解析、批注和修订。

Python 不负责：

- 用户登录。
- 用户权限。
- 合同状态。
- 版本号生成。
- 审计日志。

选择原因：

- Python 文档处理生态更成熟。
- FastAPI 写法清晰。
- 适合做内部无状态 Document Engine。

## 6. 整体架构

```text
浏览器 React
    ↓
Nginx
    ↓ /api
Go 业务后端
    ├── MySQL
    ├── 阿里云 OSS
    └── HTTP 内部调用
Python Document Engine
```

架构原则：

- React 只调用 Go。
- Go 是唯一对外业务入口。
- Python 只作为内部文档处理服务。
- Python 不直接暴露公网。
- Python 不直接处理用户权限和业务状态。

## 7. 核心业务流程

### 7.1 登录流程

```text
用户输入账号密码
    ↓
Go 校验 sys_user
    ↓
密码 Hash 校验通过
    ↓
生成 JWT
    ↓
前端保存登录态
    ↓
进入 Dashboard
```

### 7.2 DOCX 导入流程

```text
前端选择 DOCX
    ↓
Go 校验文件类型和大小
    ↓
Go 上传原始 DOCX 到阿里云 OSS
    ↓
Go 调用 Python parse
    ↓
Python 返回结构化 JSON
    ↓
Go 创建合同 V1 版本
    ↓
前端进入合同编辑页
```

原始 DOCX 永远不要覆盖。

### 7.3 编辑与版本流程

```text
用户编辑合同内容
    ↓
前端提交结构化 JSON
    ↓
Go 获取上一版本 JSON
    ↓
Go 调用 Python diff
    ↓
Python 返回变更记录
    ↓
Go 创建新版本
    ↓
Go 写入 contract_change
    ↓
Go 写入 contract_audit_log
```

历史版本必须只新增，不允许覆盖。

### 7.4 分享协作流程

```text
OWNER 点击分享
    ↓
Go 生成随机 token
    ↓
写入 contract_share
    ↓
外部客户打开分享链接
    ↓
输入姓名
    ↓
加入 contract_collaborator
    ↓
查看或编辑合同
```

分享 token 不能使用合同 ID、时间戳等可猜测内容拼接。

### 7.5 合同确认流程

```text
内部用户确认当前版本
    ↓
写入 contract_confirmation
    ↓
外部客户确认当前版本
    ↓
写入 contract_confirmation
    ↓
合同状态变为已确认
```

如果确认后还要修改：

```text
已确认
    ↓
重新开启协作
    ↓
协作中
    ↓
继续生成新版本
```

### 7.6 最终 DOCX 下载流程

```text
用户点击下载
    ↓
Go 校验权限
    ↓
Go 获取当前确认版本 JSON
    ↓
Go 调用 Python render
    ↓
Python 生成 DOCX
    ↓
Go 返回下载
```

## 8. 建议目录结构

### 8.1 前端目录

```text
contract-web/
  src/
    api/
      authApi.ts
      contractApi.ts
      dashboardApi.ts
    components/
    pages/
      Login/
      Dashboard/
      ContractList/
      ContractDetail/
      ContractEditor/
      ShareAccess/
    router/
    stores/
    types/
    utils/
```

### 8.2 Go 后端目录

```text
contract-go/
  cmd/
    server/
      main.go
  docs/
  internal/
    config/
    auth/
    database/
    middleware/
    model/
    repository/
    service/
    handler/
    oss/
    docengine/
    audit/
  pkg/
    response/
    errors/
```

目录职责：

- `handler`：处理 HTTP 请求和参数校验。
- `service`：业务逻辑。
- `repository`：数据库访问。
- `middleware`：JWT、日志、异常处理。
- `auth`：JWT 生成和解析。
- `database`：MySQL 连接池。
- `oss`：阿里云 OSS 封装。
- `docengine`：调用 Python 服务。
- `audit`：审计日志。
- `docs`：Swagger 自动生成的接口文档，由 `swag init` 生成，不手工编辑。

### 8.3 Python 文档服务目录

```text
document-engine/
  app/
    main.py
    schemas.py
    services/
      docx_parser.py
      docx_diff.py
      docx_renderer.py
    utils/
      file_check.py
  requirements.txt
```

目录职责：

- `main.py`：FastAPI 入口。
- `schemas.py`：请求和响应模型。
- `docx_parser.py`：DOCX 转 JSON。
- `docx_diff.py`：版本 Diff。
- `docx_renderer.py`：JSON 转 DOCX。
- `file_check.py`：文件校验。

## 9. Go 与 Python 职责边界

| 事项 | Go 负责 | Python 负责 |
|---|---|---|
| 用户登录 | 是 | 否 |
| JWT 鉴权 | 是 | 否 |
| 合同权限 | 是 | 否 |
| 合同状态 | 是 | 否 |
| 版本号 | 是 | 否 |
| DOCX 解析 | 调用 Python | 是 |
| Diff 计算 | 调用 Python | 是 |
| 最终 DOCX 生成 | 调用 Python | 是 |
| OSS 存储 | 是 | 否 |
| 审计日志 | 是 | 否 |

核心原则：

> Python 只处理文档，不处理业务。

这样后期就算替换文档处理方案，也不会影响合同业务主流程。

## 10. 关键 API 规划

### 10.1 认证

```text
POST /api/auth/login
GET  /api/auth/me
```

### 10.2 合同

```text
GET  /api/contracts
POST /api/contracts
GET  /api/contracts/{id}
POST /api/contracts/{id}/upload
```

### 10.3 编辑和版本

```text
PUT  /api/contracts/{id}/draft
POST /api/contracts/{id}/versions
GET  /api/contracts/{id}/versions
GET  /api/contracts/{id}/changes
```

### 10.4 分享协作

```text
POST /api/contracts/{id}/share
GET  /api/share/{token}
POST /api/share/{token}/join
POST /api/share/{token}/versions
```

### 10.5 确认和下载

```text
POST /api/contracts/{id}/confirm
POST /api/share/{token}/confirm
GET  /api/contracts/{id}/download
```

### 10.6 Dashboard

```text
GET /api/dashboard/summary
```

### 10.7 Swagger 接口文档

- 使用 `swaggo/swag` + `gin-swagger` 自动生成接口文档。
- 所有 Handler 方法必须写 Swagger 注释。
- 新增或修改接口后，执行 `swag init` 重新生成 `docs/` 目录。
- 本地访问地址：`http://localhost:8080/swagger/index.html`
- Swagger JSON 地址：`http://localhost:8080/swagger/doc.json`

常用 Swagger 注释：

```go
// @Summary 接口摘要
// @Description 接口详细说明
// @Tags 分组名称
// @Accept json
// @Produce json
// @Param request body RegisterRequest true "请求参数"
// @Success 200 {object} response.Body "成功响应"
// @Failure 400 {object} response.Body "参数错误"
// @Security BearerAuth
// @Router /api/auth/register [post]
```

注意：本项目 Swagger 不设置 `@BasePath`，`@Router` 中必须写完整路径（包含 `/api` 前缀），避免 Swagger UI 自动拼接后出现 `/api/api/...`。

## 11. DOCX 数据流

```text
原始 DOCX
    ↓
上传 OSS
    ↓
Python parse
    ↓
结构化 JSON
    ↓
前端编辑
    ↓
Python diff
    ↓
版本快照 + 变更记录
    ↓
合同确认
    ↓
Python render
    ↓
最终 DOCX 下载
```

数据保存建议：

- 原始 DOCX：保存到 OSS。
- 当前合同信息：保存到 `contract`。
- 每个版本完整快照：保存到 `contract_version`。
- 版本之间变更：保存到 `contract_change`。
- 协作者：保存到 `contract_collaborator`。
- 分享链接：保存到 `contract_share`。
- 确认记录：保存到 `contract_confirmation`。
- 操作日志：保存到 `contract_audit_log`。

## 12. 开发阶段

### 阶段 0：基础工程

开发内容：

- 初始化 React 项目。
- 初始化 Go 项目。
- 初始化 Python FastAPI 项目。
- 配置 MySQL。
- 配置 OSS 参数。
- 约定统一响应格式。
- 接入 Swagger 接口文档。

验收点：

- 前端能启动。
- Go 能启动。
- Python 能启动。
- Go 能连接 MySQL。
- Python 有 `/health` 接口。
- 访问 `/swagger/index.html` 能看到接口文档。

### 阶段 1：登录和合同 CRUD

开发内容：

- 用户登录。
- JWT 生成和校验。
- 合同创建。
- 合同列表。
- 合同详情。

验收点：

- 用户能登录。
- 未登录不能访问合同接口。
- 登录后能创建合同。
- 用户只能看到自己相关的合同。

### 阶段 2：OSS 上传

开发内容：

- 前端上传 DOCX。
- Go 校验文件类型和大小。
- Go 上传到阿里云 OSS。
- 保存 OSS object_key。

验收点：

- 只能上传 DOCX。
- 超大文件会被拒绝。
- 上传成功后 OSS 中能看到文件。
- 合同状态变为已导入。

### 阶段 3：Python parse

开发内容：

- Python 解析 DOCX。
- Python 返回结构化 JSON。
- Go 调用 Python。
- Go 保存 V1 版本。

验收点：

- 导入后能看到合同内容。
- `contract_version` 中有 V1。
- 解析失败有清晰错误提示。

### 阶段 4：在线编辑和版本

开发内容：

- 前端展示结构化 JSON。
- 前端支持基础编辑。
- Go 保存新版本。
- 版本号自增。

验收点：

- 每次保存生成新版本。
- 历史版本不可覆盖。
- 能查看版本列表。

### 阶段 5：Diff 和变更记录

开发内容：

- Python 比较旧 JSON 和新 JSON。
- Go 保存变更记录。
- 前端展示变更记录。

验收点：

- 能看到修改人。
- 能看到修改时间。
- 能看到原内容和新内容。

### 阶段 6：分享协作

开发内容：

- 生成分享 token。
- 外部客户通过链接访问。
- 客户输入姓名加入协作。
- 客户可以查看或提交修改。

验收点：

- token 随机且不可猜测。
- 过期 token 不可访问。
- 客户无需注册即可参与。
- 协作者信息被记录。

### 阶段 7：合同确认

开发内容：

- 内部用户确认。
- 外部客户确认。
- 写入确认记录。
- 更新合同状态。

验收点：

- 确认记录完整。
- 状态能进入已确认。
- 确认后再次修改需要重新进入协作中。

### 阶段 8：Dashboard

开发内容：

- 合同总数。
- 已导入数量。
- 已分享数量。
- 协作中数量。
- 已确认数量。
- 已完成数量。

验收点：

- Dashboard 数据和数据库一致。
- 状态新增时后续容易扩展。

### 阶段 9：部署

开发内容：

- 前端 npm build。
- Nginx 部署前端。
- Go 交叉编译。
- systemd 管理 Go。
- 服务器安装 Python。
- venv 安装 Python 依赖。
- systemd 管理 Python。
- Nginx 反向代理 Go API。

验收点：

- 公网访问前端正常。
- `/api` 正常代理到 Go。
- Python 只监听 `127.0.0.1` 或内网。
- Go 可以调用 Python。

## 13. 安全原则

### 13.1 JWT

- 登录后签发 JWT。
- Go 中间件统一解析 JWT。
- JWT secret 不能写入代码仓库。
- 过期时间不要设置太长。

### 13.2 密码

- 禁止明文保存密码。
- 建议使用 bcrypt。
- 登录失败不要提示“账号存在但密码错”这类过细信息。

### 13.3 分享 token

- 使用安全随机数生成。
- 不要用合同 ID 拼接。
- 不要用时间戳拼接。
- 必须设置过期时间。
- 可以支持手动失效。

### 13.4 文件安全

- 只允许 `.docx`。
- 校验文件大小。
- 校验 MIME。
- 拒绝空文件。
- 原始文件不覆盖。

### 13.5 OSS 权限

- OSS bucket 不建议全公开。
- 下载建议走 Go 鉴权。
- 如使用签名 URL，必须设置短过期时间。
- OSS AccessKey 不写入代码。

### 13.6 SQL 安全

- 使用参数化查询或 ORM。
- 不拼接用户输入到 SQL。
- 分页查询必须限制 pageSize。
- 常用查询字段要考虑索引。

### 13.7 审计

必须记录：

- 登录。
- 上传 DOCX。
- 编辑合同。
- 创建版本。
- 创建分享链接。
- 外部客户加入。
- 合同确认。
- 下载最终 DOCX。
- 权限拒绝。

## 14. 异常处理和日志

### 14.1 Go 日志字段

建议记录：

- `request_id`
- `user_id`
- `contract_id`
- `action`
- `duration_ms`
- `error`

### 14.2 Python 日志字段

建议记录：

- `request_id`
- `doc_task`
- `duration_ms`
- `error`

### 14.3 用户提示

不要把服务器内部错误直接展示给用户。

示例：

- 文件上传失败，请稍后重试。
- 文档解析失败，请检查 DOCX 内容。
- 你没有权限访问该合同。
- 分享链接已过期。

## 15. Git 分支和提交建议

分支建议：

- `main`：稳定代码。
- `dev`：日常开发集成。
- `feature/login`：登录功能。
- `feature/contract-upload`：合同上传。
- `feature/share`：分享协作。

提交信息使用 Conventional Commits。

示例：

```text
feat: add jwt login api
feat: add contract docx upload to oss
feat: add document parse integration
feat: add version diff records
fix: reject expired share token
docs: add deployment checklist
```

## 16. 本地开发环境

建议安装：

- Node.js LTS
- Go 1.22+
- Python 3.12+
- MySQL 8.x

前端：

```bash
npm install
npm run dev
```

Go：

```bash
go mod tidy
go run ./cmd/server
```

Swagger 文档生成：

```bash
# 安装 swag 命令行工具（第一次）
go install github.com/swaggo/swag/cmd/swag@v1.16.6

# 在 backend 目录下重新生成接口文档
swag init -g cmd/server/main.go -o docs --parseDependency --parseInternal
```

接口文档地址：`http://localhost:8080/swagger/index.html`

Python：

```bash
python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --host 127.0.0.1 --port 9002 --reload
```

## 17. 服务器部署规划

### 17.1 前端部署

开发机执行：

```bash
npm install
npm run build
```

将 `dist/` 上传到服务器：

```text
/var/www/lshc-contract-web
```

由 Nginx 对外提供访问。

### 17.2 Go 交叉编译

在 macOS 开发机编译 Linux 二进制。

如果服务器是 x86_64：

```bash
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o contract-server ./cmd/server
```

如果服务器是 ARM64：

```bash
GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -o contract-server ./cmd/server
```

上传到服务器：

```text
/opt/lshc/contract-go/contract-server
```

### 17.3 Python 服务器环境

服务器执行：

```bash
cd /opt/lshc/document-engine
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

`requirements.txt` V1 可以先保持简单：

```text
fastapi
uvicorn
python-docx
pydantic
```

### 17.4 Go systemd 示例

```ini
[Unit]
Description=LSHC Contract Go API
After=network.target mysql.service

[Service]
WorkingDirectory=/opt/lshc/contract-go
ExecStart=/opt/lshc/contract-go/contract-server
Restart=always
RestartSec=3
EnvironmentFile=/opt/lshc/contract-go/.env

[Install]
WantedBy=multi-user.target
```

### 17.5 Python systemd 示例

```ini
[Unit]
Description=LSHC Document Engine
After=network.target

[Service]
WorkingDirectory=/opt/lshc/document-engine
ExecStart=/opt/lshc/document-engine/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 9002
Restart=always
RestartSec=3
EnvironmentFile=/opt/lshc/document-engine/.env

[Install]
WantedBy=multi-user.target
```

### 17.6 Nginx 示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/lshc-contract-web;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

部署要求：

- Nginx 对外提供前端。
- Nginx 反向代理 Go API。
- Python 只监听 `127.0.0.1` 或内网。
- Python 不直接暴露公网。

## 18. 上线检查清单

- 前端能正常打开。
- 登录功能正常。
- Go 服务 systemd 状态为 `active`。
- Python 服务 systemd 状态为 `active`。
- Nginx 配置检查通过。
- `/api` 能正确代理到 Go。
- Go 能访问 MySQL。
- Go 能访问 OSS。
- Go 能调用 Python。
- 上传非 DOCX 会被拒绝。
- 分享 token 过期后不可访问。
- 历史版本不可覆盖。
- 关键动作写入审计日志。
- 最终 DOCX 能下载并打开。
- `.env` 不进入代码仓库。

## 19. V1 Definition of Done

V1 完成标准：

- 用户可以登录。
- 用户可以导入 DOCX。
- 原始 DOCX 保存到 OSS。
- DOCX 可以被解析为结构化 JSON。
- 用户可以在线编辑合同内容。
- 每次保存生成新版本。
- 版本之间有变更记录。
- OWNER 可以生成分享链接。
- 外部客户无需注册即可参与协作。
- 内部用户和外部客户可以确认合同。
- Dashboard 展示合同数量和状态。
- 当前版本可以生成最终 DOCX 下载。
- 权限、分享 token、文件校验、审计日志都已实现。
- 前端、Go、Python 都可以在服务器上稳定运行。

## 20. V2 扩展方向

V1 稳定后再考虑：

- 手机号验证码登录。
- 客户实名信息。
- 公司成员和部门权限。
- 合同模板库。
- 合同变量自动填充。
- 电子签章对接。
- AI 风险提示。
- 批注和修订模式。
- 消息通知。
- Dashboard 趋势图和逾期提醒。

## 21. 推荐开发节奏

建议按 4 周推进：

第 1 周：

- 登录。
- 合同 CRUD。
- 前端基础页面。

第 2 周：

- OSS 上传。
- Python parse。
- 编辑和版本保存。

第 3 周：

- Diff。
- 分享协作。
- 合同确认。
- Dashboard。
- 最终 DOCX 下载。

第 4 周：

- 异常处理。
- 审计日志。
- 部署。
- 上线检查。
- 小范围试用。

## 22. 最重要的开发提醒

后续写 Go 和 Python 时，不要追求复杂语法。

优先做到：

- 文件结构清楚。
- 方法职责单一。
- 命名语义明确。
- 关键逻辑写中文注释。
- 先跑通端到端流程。
- 每阶段都能验收。

V1 不是为了证明架构复杂，而是为了尽快做出一个真实可用、可维护、可上线的合同协作系统。

